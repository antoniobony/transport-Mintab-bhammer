export type nodeType = {
    id: number,
    label: string,
    title: string,
    x: number, // Colonne des alphabets
    y: number, // Espacement vertical
    group: string,
    otherLabel:string,

}